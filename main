<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>당곡고 웹 프로그래밍 실습 - 트럼프 카드 업&다운 게임</title>
    <!-- 귀여운 폰트 적용 -->
    <link href="https://fonts.googleapis.com/css2?family=Gowun+Dodum&display=swap" rel="stylesheet">
    <style>
        /* CSS 스타일 정의: 게임의 레이아웃과 디자인을 담당합니다. */
        body {
            font-family: 'Gowun Dodum', sans-serif;
            background: linear-gradient(135deg, #1e272c, #0f171e);
            color: #ecf0f1;
            margin: 0;
            padding: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
            min-height: 100vh;
        }

        .game-container {
            background-color: #2c3e50;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
            width: 100%;
            max-width: 600px;
            padding: 25px;
            box-sizing: border-box;
            text-align: center;
        }

        h1 {
            color: #f1c40f;
            margin-top: 0;
            font-size: 24px;
        }

        .dashboard {
            display: flex;
            justify-content: space-between;
            background: #34495e;
            padding: 15px;
            border-radius: 10px;
            margin-bottom: 20px;
            font-size: 16px;
        }

        .gold-display {
            color: #f1c40f;
            font-weight: bold;
        }

        /* 트럼프 카드 스타일 정의 */
        .card-zone {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin: 20px 0;
            min-height: 220px;
        }

        .card {
            width: 140px;
            height: 200px;
            background: white;
            border-radius: 12px;
            box-shadow: 0 8px 16px rgba(0,0,0,0.3);
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            padding: 12px;
            box-sizing: border-box;
            border: 2px solid #bdc3c7;
            position: relative;
            transition: transform 0.3s ease;
        }

        .card.red {
            color: #e74c3c;
        }

        .card.black {
            color: #2c3e50;
        }

        .card-corner {
            font-size: 22px;
            font-weight: bold;
            line-height: 1;
        }

        .card-suit-center {
            font-size: 64px;
            text-align: center;
            align-self: center;
        }

        /* 베팅 및 조작 영역 */
        .bet-control {
            margin-bottom: 20px;
        }

        .bet-input-group {
            display: flex;
            justify-content: center;
            gap: 8px;
            margin-top: 10px;
        }

        input[type="number"] {
            background-color: #34495e;
            border: 2px solid #bdc3c7;
            color: white;
            padding: 8px 12px;
            border-radius: 8px;
            width: 120px;
            font-size: 16px;
            text-align: center;
        }

        .btn-ratio {
            background-color: #7f8c8d;
            border: none;
            color: white;
            padding: 8px 12px;
            border-radius: 8px;
            cursor: pointer;
            font-weight: bold;
        }

        .btn-ratio:hover {
            background-color: #95a5a6;
        }

        /* 업, 다운, 페어 버튼 레이아웃 */
        .action-zone {
            display: grid;
            grid-template-columns: 1fr 1fr 1fr;
            gap: 12px;
            margin-bottom: 20px;
        }

        .btn-action {
            border: none;
            padding: 15px;
            font-size: 16px;
            font-weight: bold;
            border-radius: 10px;
            cursor: pointer;
            transition: transform 0.1s, opacity 0.3s;
            color: white;
            box-shadow: 0 4px 6px rgba(0,0,0,0.2);
        }

        .btn-action:active:not(:disabled) {
            transform: scale(0.95);
        }

        .btn-action:disabled {
            opacity: 0.4;
            cursor: not-allowed;
        }

        .btn-up { background-color: #e74c3c; } /* Red */
        .btn-up:hover:not(:disabled) { background-color: #c0392b; }

        .btn-down { background-color: #3498db; } /* Blue */
        .btn-down:hover:not(:disabled) { background-color: #2980b9; }

        .btn-pair { background-color: #9b59b6; } /* Purple */
        .btn-pair:hover:not(:disabled) { background-color: #8e44ad; }

        .odds-text {
            display: block;
            font-size: 13px;
            margin-top: 5px;
            color: #f1c40f;
        }

        /* 결과 연출 레이어 */
        .result-panel {
            background-color: #1abc9c;
            border-radius: 12px;
            padding: 15px;
            margin-top: 20px;
            display: none;
            animation: fadeIn 0.5s ease;
        }

        .result-panel.lose {
            background-color: #e67e22;
        }

        .btn-next {
            background-color: #2ecc71;
            color: white;
            border: none;
            padding: 10px 25px;
            font-size: 16px;
            border-radius: 8px;
            cursor: pointer;
            font-weight: bold;
            margin-top: 10px;
        }

        .btn-next:hover {
            background-color: #27ae60;
        }

        .history-section {
            margin-top: 25px;
            text-align: left;
            background: #1e272c;
            padding: 15px;
            border-radius: 10px;
            max-height: 150px;
            overflow-y: auto;
        }

        .history-title {
            color: #bdc3c7;
            font-size: 14px;
            margin-bottom: 8px;
            border-bottom: 1px solid #34495e;
            padding-bottom: 4px;
        }

        .history-item {
            font-size: 13px;
            margin-bottom: 4px;
            display: flex;
            justify-content: space-between;
        }

        .history-win { color: #2ecc71; }
        .history-lose { color: #e74c3c; }

        .free-charge {
            background-color: #e67e22;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 10px;
            cursor: pointer;
            font-weight: bold;
            margin-top: 10px;
            display: none;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(-10px); }
            to { opacity: 1; transform: translateY(0); }
        }
    </style>
